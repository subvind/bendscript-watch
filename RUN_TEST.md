# run test local
```bash
# install
npm install

npm start -- -i ./test/input -o ./test/output
```

# run test packaged
```bash
# install global
npm install transbend -g

transbend -i ./test/input -o ./test/output
```